---
title: "Assignment 6 - Who were the Winners?"
author: "Haylee Oyler"
format: html
execute:
    warning: false
    message: false
---

```{r}
# Load libraries
library(dbplyr)
library(tidyverse)
library(DBI)
library(duckdb)
```

```{r}
conn <- DBI::dbConnect(duckdb(), dbdir = "database/database.db")
```

```{r}
DBI::dbListTables(conn)
```

```{r}
personnel <- tbl(conn, "Personnel")
personnel

bird_eggs <- tbl(conn, "Bird_eggs")
bird_eggs

camp_assignment <- tbl(conn, "Camp_assignment")
camp_assignment

site <- tbl(conn, "Site")
site

bird_nests <- tbl(conn, "Bird_nests")
bird_nests
```